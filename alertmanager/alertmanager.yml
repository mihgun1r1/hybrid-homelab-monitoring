route:
  group_by: ['alertname']
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 10m
  receiver: 'ai-incident-webhook'

receivers:
  - name: 'ai-incident-webhook'
    webhook_configs:
      - url: 'http://ai_responder:5000/webhook'
        send_resolved: true